# إرشادات الأمان

## 🔐 حماية مفاتيح Firebase API

### المشكلة
يحتوي هذا المشروع على مفاتيح Firebase API في ملف `firebase-config.js`. هذه المفاتيح حساسة ويجب التعامل معها بحذر.

### الحلول الموصى بها

#### 1. للاستخدام المحلي فقط
إذا كان المشروع للاستخدام الشخصي أو التعليمي، يمكنك الاحتفاظ بالمفاتيح كما هي.

#### 2. للنشر العام
إذا كنت تريد نشر المشروع بشكل عام، اتبع هذه الخطوات:

```bash
# 1. أضف ملف التكوين إلى .gitignore
echo "firebase-config.js" >> .gitignore

# 2. أنشئ ملف تكوين نموذجي
cp firebase-config.js firebase-config.example.js

# 3. استبدل المفاتيح الحقيقية بنصوص وهمية في الملف النموذجي
```

#### 3. استخدام متغيرات البيئة (الأفضل)
```javascript
// firebase-config.js
const firebaseConfig = {
    apiKey: process.env.FIREBASE_API_KEY || "your-api-key-here",
    authDomain: process.env.FIREBASE_AUTH_DOMAIN || "your-domain.firebaseapp.com",
    // ... باقي المتغيرات
};
```

### قواعد Firebase Security Rules
تأكد من تكوين قواعد الأمان في Firestore بشكل صحيح:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // قواعد محددة للتعليقات
    match /comments/{document} {
      allow read: if true;
      allow write: if request.auth != null || 
                      (request.resource.data.keys().hasAll(['name', 'comment', 'timestamp']) &&
                       request.resource.data.name is string &&
                       request.resource.data.comment is string);
    }
  }
}
```

## 🛡️ نصائح أمنية إضافية

1. **لا تشارك مفاتيح API الحقيقية** في المستودعات العامة
2. **استخدم HTTPS** دائماً في الإنتاج
3. **راجع قواعد Firebase** بانتظام
4. **راقب الاستخدام** في وحدة تحكم Firebase
5. **فعّل التنبيهات** للاستخدام غير المعتاد

## 📞 الإبلاغ عن مشاكل أمنية

إذا وجدت مشكلة أمنية، يرجى التواصل مباشرة عبر:
- البريد الإلكتروني: yjmt46999@gmail.com
- لا تنشر المشاكل الأمنية في Issues العامة
